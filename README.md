# docker-mem-leak - Simple program to exhaust memory in a docker container (and by-proxy a host)

- Loops to infinity appending pi to a list (array)

Basic usage
------------------

$ docker-mem-leak.py